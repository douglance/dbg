{
	"name": "@douglance/dbg",
	"version": "0.4.0",
	"description": "Stateless, non-interactive debugger CLI for automation (CDP + LLDB DAP).",
	"license": "MIT",
	"type": "module",
	"bin": {
		"dbg": "bin/dbg"
	},
	"files": [
		"bin",
		"dist",
		"README.md",
		"LICENSE"
	],
	"engines": {
		"node": ">=22"
	},
	"dependencies": {
		"@douglance/play": "^0.2.0",
		"chrome-remote-interface": "^0.34.0"
	},
	"scripts": {
		"build": "node scripts/build.mjs",
		"prepack": "node scripts/build.mjs"
	},
	"publishConfig": {
		"access": "public"
	}
}
